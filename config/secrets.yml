# Historical Rails reference only. The supported photo workflow does not load
# this file. No secret values belong in source control.
development:
  secret_key_base: <%= ENV["LEGACY_SECRET_KEY_BASE"] %>
test:
  secret_key_base: <%= ENV["LEGACY_TEST_SECRET_KEY_BASE"] %>
production:
  secret_key_base: <%= ENV["LEGACY_SECRET_KEY_BASE"] %>
