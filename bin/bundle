#!/usr/bin/env ruby
warn "The vendored Rails 4 Bundler launcher is retired; use the system bundler only for the dependency-free Gemfile."
exit 78
ENV['BUNDLE_GEMFILE'] ||= File.expand_path('../../Gemfile', __FILE__)
load Gem.bin_path('bundler', 'bundle')
